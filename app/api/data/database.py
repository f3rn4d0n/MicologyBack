#from databases import Database
#from sqlalchemy import create_engine

#DATABASE_URL = "postgresql://username:password@localhost/dbname"

#database = Database(DATABASE_URL)
#metadata = create_engine(DATABASE_URL).metadata
